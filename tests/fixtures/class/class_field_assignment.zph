class Point {
	x: number;
	y: number;
	constructor(x: number, y: number) {
		self.x = x;
		self.y = y;
	}
	fn moveX(dx: number): number {
		self.x = self.x + dx;
		return self.x;
	}
}

var p: Point = Point(2, 3);
p.x = 10;
return p.moveX(5);
