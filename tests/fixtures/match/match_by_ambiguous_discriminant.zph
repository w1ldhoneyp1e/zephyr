class Dog {
	kind: string;

	constructor() {
		self.kind = "pet";
	}
}

class Cat {
	kind: string;

	constructor() {
		self.kind = "pet";
	}
}

fn speak(animal: Dog | Cat): number {
	return match animal by kind {
		pet => 1,
	};
}

return speak(Dog());
