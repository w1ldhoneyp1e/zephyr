class Dog {
	kind: string;

	constructor() {
		self.kind = "dog";
	}
}

class Cat {
	kind: string;

	constructor() {
		self.kind = "cat";
	}
}

fn speak(animal: Dog | Cat): number {
	return match animal by kind {
		dog => 1,
		dog => 2,
		cat => 3,
	};
}

return speak(Dog());
