class Counter {
	private value: number;
	constructor(value: number) {
		self.value = value;
	}

	fn inc(): number {
		self.value = self.value + 1;
		return self.value;
	}
}

var counter: Counter = Counter(4);
return counter.inc();
