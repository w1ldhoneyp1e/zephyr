class Box {
	value: number;

	constructor(value: number) {
		self.value = value;
	}

	fn get(): number {
		return self.value;
	}
}

var box: Box | null = Box(4);
var value: number = box?.get();

return value;
