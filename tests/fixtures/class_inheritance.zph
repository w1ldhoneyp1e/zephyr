class Animal {
	public age: number;
	constructor(age: number) {
		self.age = age;
	}

	fn grow(): number {
		self.age = self.age + 1;
		return self.age;
	}
}

class Dog extends Animal {
	public weight: number;
	constructor(age: number, weight: number) {
		super(age);
		self.weight = weight;
	}

	fn total(): number {
		return self.age + self.weight;
	}
}

var dog: Dog = Dog(2, 3);
return dog.grow() + dog.total();
