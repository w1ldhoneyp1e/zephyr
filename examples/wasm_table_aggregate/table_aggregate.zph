type SaleRow = {
	id: number;
	amount: number;
	active: boolean;
};

fn sumActiveAmount(rows: SaleRow[], length: number): number {
	var total: number = 0;
	for (i in 0..length) {
		if (rows[i].active) {
			total = total + rows[i].amount;
		}
	}
	return total;
}
